---
label: AGAINST_FAILURES
standing: user
force: should
authority: 'user ruling, 2026-09-01: "heuristics look good."'
why:
  - ../design.kb/experience.md
  - ../design.kb/goals.kb/delegation-is-an-exchange.md
---

# Design against the failure classes, not a feature list

Design against the failure classes, not a feature list. The lived ones
are `experience.kb/`; the anticipated ones are named by the goal that
prevents each. A feature that prevents no failure is weight.
