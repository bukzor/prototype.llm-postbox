---
label: POSTBOX_KB
standing: agent
verdict: declined
why:
  - runtime-root-is-grafted-xdg-state.md
---

# The postbox as `postbox.kb/`

Reuses the suffix every agent already reads as "structured, worth
loading". But `.kb` means durable knowledge under governance, and
ephemeral traffic filed under it dilutes the suffix everywhere else
it appears.
