---
label: TUI
standing: user
authority: user testimony, 2026-09-01
---

# The tui setting is refused in an agent

`/tui default` is flatly refused inside an agent. The person needs
`default` for their tmux workflows, so an agent cannot be driven the way
their sessions are.
