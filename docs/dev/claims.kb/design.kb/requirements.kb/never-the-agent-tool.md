---
label: NO_AGENT_TOOL
standing: agent
authority: >-
  proposed by the user, 2026-09-01: "Perhaps an acceptance criteria
  should/could be: no new agent*jsonl files appearing in
  ~/.claude/project"; signed agent because "should/could" was an offer,
  not a ruling
why:
  - ../goals.kb/a-delegate-lacks-nothing-a-session-has.md
  - ../mission.kb/delegation-is-avoided-today.md
---

# No batch creates an agent transcript

After a batch runs, no new agent transcript files (`agent*.jsonl`) have
appeared under the claude-code projects directory.

This is the one test proving the mechanism never routes through the
built-in agent tool, whose every lived failure (`experience.kb/`) the
person refuses to pay again.
